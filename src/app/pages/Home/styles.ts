import { cva } from "class-variance-authority";

export const homeContainer = cva('mt-4');

export const inputContainer = cva('flex items-center justify-center mb-4');

export const cardContainer = cva('flex flex-wrap justify-center  space-y-4 space-x-4 list-container');

export const button = cva('flex justify-center mt-4 mb-4');